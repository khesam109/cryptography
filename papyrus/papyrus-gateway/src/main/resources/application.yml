spring:
  servlet:
    multipart:
      enabled: true
      file-size-threshold: 2KB
      max-file-size: 1MB
      max-request-size: 10MB

storage:
  location: upload-dir

server:
  port: 8080
  servlet:
    context-path: /papyrus/gateway/api